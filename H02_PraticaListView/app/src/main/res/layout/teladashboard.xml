<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center_horizontal"
    android:id="@+id/telainicial_drawer"
    android:orientation="vertical"
    android:padding="20dp">

    <Button
        android:id="@+id/teladashboard_bt_clientes"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_marginTop="20dp"
        android:background="@drawable/buttons"
        android:text="Clientes"
        android:textColor="#FFFFFFFF"
        android:textSize="22sp" />

    <Button
        android:id="@+id/teladashboard_bt_itens"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_marginTop="20dp"
        android:background="@drawable/buttons"
        android:text="Produtos"
        android:textColor="#FFFFFFFF"
        android:textSize="22sp" />


    <Button
        android:id="@+id/teladashboard_bt_pedidos"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_marginTop="20dp"
        android:background="@drawable/buttons"
        android:text="Pedidos"
        android:textColor="#FFFFFFFF"
        android:textSize="22sp" />

    <Button
        android:id="@+id/teladashboard_bt_sinc_pedidos"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginTop="20dp"
        android:background="@drawable/buttons"
        android:text="Sincronizar Pedidos"
        android:textColor="#FFFFFFFF" />

</LinearLayout>